import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiInputAdornmentStyles: Components<MuiTheme>["MuiInputAdornment"] = {
    styleOverrides: {
        root: ({ theme }) => ({
            //
        })
    }
}