import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiScopedCssBaselineStyles: Components<MuiTheme>["MuiScopedCssBaseline"] = {
    styleOverrides: {
    }
}