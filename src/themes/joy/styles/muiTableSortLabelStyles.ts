import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiTableSortLabelStyles: Components<MuiTheme>["MuiTableSortLabel"] = {
    styleOverrides: {
    }
}