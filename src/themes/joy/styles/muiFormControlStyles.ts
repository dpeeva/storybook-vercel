import { Components } from "@mui/material"
// import { formHelperTextClasses } from "@mui/material/FormHelperText"
import { MuiTheme } from "../../../typings"

export const muiFormControlStyles: Components<MuiTheme>["MuiFormControl"] = {
    styleOverrides: {
        root: ({ theme }) => ({
            //
        })
    }
}