import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiToggleButtonGroupStyles: Components<MuiTheme>["MuiToggleButtonGroup"] = {
    styleOverrides: {
    }
}