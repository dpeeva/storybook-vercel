import { Components } from "@mui/material"
// import { svgIconClasses } from "@mui/material/SvgIcon"
import { MuiTheme } from "../../../typings"

export const muiTablePaginationStyles: Components<MuiTheme>["MuiTablePagination"] = {
    styleOverrides: {
        root: ({ theme }) => ({
        }),
        displayedRows: {
        },
        selectLabel: ({ theme }) => ({
        }),
    }
}