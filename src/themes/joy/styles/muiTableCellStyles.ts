import { Components } from "@mui/material"
// import { tableCellClasses } from "@mui/material/TableCell"
import { MuiTheme } from "../../../typings"

export const muiTableCellStyles: Components<MuiTheme>["MuiTableCell"] = {
    styleOverrides: {
        root: ({ theme }) => ({
        }),
        head: {
        },
        footer: {
        }
    }
}