import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiListItemStyles: Components<MuiTheme>["MuiListItem"] = {
    variants: [],
    styleOverrides: {
        root: ({ theme }) => ({
        })
    }
}