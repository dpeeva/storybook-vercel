import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiImageListItemBarStyles: Components<MuiTheme>["MuiImageListItemBar"] = {
    styleOverrides: {
    }
}