import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiPaginationItemStyles: Components<MuiTheme>["MuiPaginationItem"] = {
    styleOverrides: {
    }
}