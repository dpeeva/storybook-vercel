import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiTabScrollButtonStyles: Components<MuiTheme>["MuiTabScrollButton"] = {
    styleOverrides: {
    }
}