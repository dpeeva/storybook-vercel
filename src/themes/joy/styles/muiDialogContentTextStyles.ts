import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiDialogContentTextStyles: Components<MuiTheme>["MuiDialogContentText"] = {
    styleOverrides: {
    }
}