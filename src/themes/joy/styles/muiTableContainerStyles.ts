import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiTableContainerStyles: Components<MuiTheme>["MuiTableContainer"] = {
    styleOverrides: {
    }
}