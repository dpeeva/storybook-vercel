import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiSwipeableDrawerStyles: Components<MuiTheme>["MuiSwipeableDrawer"] = {
    defaultProps: {
    }
}