import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiSpeedDialActionStyles: Components<MuiTheme>["MuiSpeedDialAction"] = {
    styleOverrides: {
    }
}