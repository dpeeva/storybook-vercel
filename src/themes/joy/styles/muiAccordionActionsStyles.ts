import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiAccordionActionsStyles: Components<MuiTheme>["MuiAccordionActions"] = {
    styleOverrides: {
    }
}