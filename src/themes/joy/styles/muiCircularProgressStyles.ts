import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiCircularProgressStyles: Components<MuiTheme>["MuiCircularProgress"] = {
    styleOverrides: {
    }
}