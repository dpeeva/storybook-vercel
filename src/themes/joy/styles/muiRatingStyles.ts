import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiRatingStyles: Components<MuiTheme>["MuiRating"] = {
    styleOverrides: {
        root: ({ theme }) => ({
        })
    }
}