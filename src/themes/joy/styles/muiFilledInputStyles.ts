import { Components } from "@mui/material"
// import { inputAdornmentClasses } from "@mui/material/InputAdornment"
import { MuiTheme } from "../../../typings"

export const muiFilledInputStyles: Components<MuiTheme>["MuiFilledInput"] = {
    styleOverrides: {
    }
}