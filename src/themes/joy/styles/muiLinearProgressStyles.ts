import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiLinearProgressStyles: Components<MuiTheme>["MuiLinearProgress"] = {
    styleOverrides: {
    }
}