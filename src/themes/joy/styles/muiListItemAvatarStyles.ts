import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiListItemAvatarStyles: Components<MuiTheme>["MuiListItemAvatar"] = {
    styleOverrides: {
    }
}