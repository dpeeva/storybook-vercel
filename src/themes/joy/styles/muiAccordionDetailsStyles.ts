import { Components } from "@mui/material"
import { MuiTheme } from "../../../typings"

export const muiAccordionDetailsStyles: Components<MuiTheme>["MuiAccordionDetails"] = {
    styleOverrides: {
    }
}